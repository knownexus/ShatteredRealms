@if (!string.IsNullOrEmpty(Message))
{
    <div class="alert @GetAlertClass()">@Message</div>
}

@code {
    [Parameter] public string? Message { get; set; }
    [Parameter] public string Type { get; set; } = "danger";

    private string GetAlertClass() => $"alert-{Type}";
}
